[package]
name = "llmoop-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
name = "llmoop_runtime"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
